import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    footerNav_container: {
        backgroundColor: '#008000',
        flexDirection: 'row',
        width: '100%',
        justifyContent: 'space-between',
        alignItems: 'center',
        paddingHorizontal: 20,
        height: 60,
    },

    footerNav_main_button:    {
        borderColor: '#008000',
        borderWidth: 15,
        borderRadius: 50,
        alignSelf: 'center',
        top: -15,
    }
});