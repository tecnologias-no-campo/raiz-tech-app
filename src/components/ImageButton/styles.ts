import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    imageButton_button: {
        width: 50,
        height: 50,
        borderRadius: '50%',
        alignItems: 'center',
        justifyContent: 'center',
    },

    imageButton_image: {
        width: 30, 
        height: 30,
        resizeMode: 'contain',
    }
});