import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    secondaryTitle_title: {
        fontFamily: 'BebasNeue-Regular',
        fontSize: 28,
        textAlign: 'center',
        marginBottom: 10        
    }
});