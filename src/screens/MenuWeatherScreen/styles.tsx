import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerAlignSecondary: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-around',
        marginBottom: 15
    }
});